# План реализации фичи: export-fn-naming

## 📋 Список задач
- [ ] Добавить обработку MemberExpression в функции `funcname()` в `src/astutil.js`
- [ ] Добавить обработку MemberExpression в функции `init()` для определения `willBeAnonymous`
- [ ] Создать тестовые файлы для проверки новой функциональности
- [ ] Обновить эталонные результаты тестов
- [ ] Итоговая проверка и тестирование

## 🔍 Анализ и исследование

### Структура кодовой базы

Проект `js-callgraph` - это инструмент статического анализа для построения графов вызовов JavaScript функций. Основные модули:

1. **src/astutil.js** - работа с AST (абстрактным синтаксическим деревом):
   - Парсинг файлов в AST
   - Функция `init()` - инициализация атрибутов узлов AST, включая определение имен функций
   - Функция `funcname()` - извлечение имени функции из узла AST
   - Функция `buildMemberExpressionName()` - построение имени из цепочки MemberExpression

2. **src/bindings.js** - разрешение имен переменных и областей видимости

3. **src/flowgraph.js** - построение графа потока данных

4. **tests/** - тестовые файлы с входными данными и эталонными результатами

### Текущая архитектура

**Проблема:**

Когда встречается конструкция `exports.fn = function () {}` или `module.exports.fn = function () {}`, текущий код не может правильно извлечь имя функции, потому что:

1. В AST эта конструкция представлена как `AssignmentExpression`, где:
   - `left` имеет тип `MemberExpression` (а не `Identifier`)
   - `left.object` = `Identifier("exports")` или `MemberExpression` для `module.exports`
   - `left.property` = `Identifier("fn")`
   - `right` = `FunctionExpression` без `id` (анонимная функция)

2. В функции `funcname()` (строка 353-356) есть проверка:
```javascript
if (parent?.type === 'AssignmentExpression') {
    if (parent?.left?.type == 'Identifier') {
        return parent.left.name;
    }
}
```

Эта проверка срабатывает только для `var fn = function() {}`, но не для `exports.fn = function() {}`.

3. В функции `init()` (строка 177-180) аналогичная проверка определяет, будет ли функция анонимной:
```javascript
if (parent?.type === 'AssignmentExpression') {
    if (parent?.left?.type == 'Identifier') {
        hasParentName = true;
    }
}
```

Из-за этого функции с `MemberExpression` слева считаются анонимными и получают имена вида `global:anon[1]`.

**Существующий паттерн для Property:**

В коде уже есть обработка похожего случая для объектных литералов (строки 144-161):
```javascript
if (nd.type === 'FunctionExpression' && parent && parent.type === 'Property') {
    if (!parent.computed) {
        if (parent.key.type === 'Identifier') {
            nd.id = parent.key;
        }
    }
}
```

Это решает проблему для `{fn: function() {}}`, но не для `exports.fn = function() {}`.

### Зависимости и точки интеграции

**Ключевые функции для изменения:**

1. **`funcname(func)` в src/astutil.js (строки 346-393)**
   - Отвечает за извлечение имени функции
   - Проверяет родительский узел для получения имени анонимной функции
   - Нужно добавить обработку `MemberExpression` в левой части `AssignmentExpression`

2. **`init(root)` в src/astutil.js (строки 72-279)**
   - Инициализирует атрибуты узлов AST
   - Определяет, будет ли функция анонимной (`willBeAnonymous`)
   - Нужно добавить обработку `MemberExpression` для корректного определения `hasParentName`

3. **`buildMemberExpressionName(node)` в src/astutil.js (строки 302-316)**
   - Уже существующая вспомогательная функция
   - Строит имя из цепочки `MemberExpression`
   - Можно использовать для извлечения имени свойства

### Что нужно учесть и возможные сложности

**Сложности:**

1. **Разные варианты MemberExpression:**
   - `exports.fn = function() {}` - простой случай, нужно вернуть `"fn"`
   - `module.exports.fn = function() {}` - нужно вернуть `"fn"`
   - `obj.nested.fn = function() {}` - нужно вернуть `"fn"` (имя последнего свойства)
   - `obj[computed] = function() {}` - computed properties, нужно игнорировать (как в существующем коде)

2. **Консистентность с существующим поведением:**
   - Для `var fn = function() {}` возвращается `"fn"`
   - Для `exports.fn = function() {}` должно возвращаться `"fn"` (не `"exports.fn"`)
   - Это соответствует логике CommonJS, где экспортируется функция с именем `fn`

3. **Обратная совместимость:**
   - Изменение может повлиять на существующие тесты
   - Нужно проверить все тесты и обновить эталонные результаты, если потребуется

4. **Arrow функции:**
   - Код должен работать и для `exports.fn = () => {}`
   - Проверка уже включает `ArrowFunctionExpression`

## 📝 План реализации

### Необходимые условия

- Node.js установлен и настроен
- Проект `js-callgraph` клонирован локально
- Установлены зависимости (`npm install`)

### Пошаговая реализация

1. **Шаг 1: Добавить обработку MemberExpression в функцию `funcname()`**
   - Файлы для изменения: `src/astutil.js`
   - Необходимые изменения:
     - Найти блок кода на строках 353-356, где обрабатывается `AssignmentExpression`
     - Добавить дополнительную проверку для случая, когда `parent.left.type === 'MemberExpression'`
     - Извлечь имя свойства из `parent.left.property`
     - Убедиться, что это работает только для не-вычисляемых свойств (`!parent.left.computed`)
   
   Пример кода:
   ```javascript
   if (parent?.type === 'AssignmentExpression') {
       if (parent?.left?.type == 'Identifier') {
           return parent.left.name;
       } else if (parent?.left?.type === 'MemberExpression') {
           // exports.fn = function() {} -> "fn"
           // module.exports.fn = function() {} -> "fn"
           if (!parent.left.computed && parent.left.property?.type === 'Identifier') {
               return parent.left.property.name;
           }
       }
   }
   ```

2. **Шаг 2: Добавить обработку MemberExpression в функцию `init()` для определения `willBeAnonymous`**
   - Файлы для изменения: `src/astutil.js`
   - Необходимые изменения:
     - Найти блок кода на строках 177-180, где определяется `hasParentName`
     - Добавить проверку для `MemberExpression` в левой части присваивания
     - Логика должна совпадать с изменениями из Шага 1
   
   Пример кода:
   ```javascript
   if (parent?.type === 'AssignmentExpression') {
       if (parent?.left?.type == 'Identifier') {
           hasParentName = true;
       } else if (parent?.left?.type === 'MemberExpression') {
           // exports.fn = function() {} should have a name
           if (!parent.left.computed && parent.left.property?.type === 'Identifier') {
               hasParentName = true;
           }
       }
   }
   ```

3. **Шаг 3: Создать тестовые файлы**
   - Файлы для создания:
     - `tests/input/basics/export-fn.js` - основной тест для `exports.fn = function() {}`
     - Опционально: `tests/input/import-export/module.exports/module-exports-fn.js` - для `module.exports.fn`
   
   - Содержимое `tests/input/basics/export-fn.js`:
   ```javascript
   exports.fn = function () {
       console.log("test");
   };
   
   exports.handler = function () {
       console.log("handler");
   };
   
   exports.arrowFn = () => {
       console.log("arrow");
   };
   
   exports.fn();
   exports.handler();
   exports.arrowFn();
   ```

4. **Шаг 4: Запустить тесты и получить актуальные результаты**
   - Файлы для изменения: нет (только чтение)
   - Необходимые действия:
     - Запустить анализ на новых тестовых файлах: `node js-callgraph.js --cg tests/input/basics/export-fn.js --strategy DEMAND`
     - Запустить анализ с ONESHOT стратегией: `node js-callgraph.js --cg tests/input/basics/export-fn.js --strategy ONESHOT`
     - Сохранить результаты для проверки

5. **Шаг 5: Создать эталонные файлы истинности (truth files)**
   - Файлы для создания:
     - `tests/reference/DEMAND/basics/export-fn.truth.json`
     - `tests/reference/ONESHOT/basics/export-fn.truth.json`
   
   - Ожидаемый результат: вызовы должны быть от `global` к функциям с именами `fn`, `handler`, `arrowFn`, а НЕ `global:anon[1]`, `global:anon[2]`, и т.д.

6. **Шаг 6: Запустить полный набор тестов**
   - Команда: `npm test`
   - Проверить, что:
     - Новые тесты проходят успешно
     - Существующие тесты не сломались
     - Если какие-то существующие тесты изменили поведение, убедиться, что это ожидаемо

7. **Шаг 7: Обновить существующие эталонные результаты (если необходимо)**
   - Файлы для проверки: все файлы в `tests/reference/`
   - Если изменения повлияли на существующие тесты с `module.exports` или похожими конструкциями, обновить их эталонные результаты
   - Документировать изменения

### Стратегия тестирования

**Модульное тестирование:**

1. Создать простой скрипт для тестирования функции `funcname()`:
```javascript
const astutil = require('./src/astutil');

const testCases = [
    { code: 'exports.fn = function() {}', expected: 'fn' },
    { code: 'module.exports.handler = function() {}', expected: 'handler' },
    { code: 'exports.arrow = () => {}', expected: 'arrow' },
    { code: 'var local = function() {}', expected: 'local' }
];

testCases.forEach(test => {
    const ast = astutil.astFromSrc('test.js', test.code);
    const funcs = astutil.getFunctions(ast, test.code);
    const actualName = funcs[0].name;
    console.log(`Code: ${test.code}`);
    console.log(`Expected: ${test.expected}, Actual: ${actualName}`);
    console.log(`Status: ${actualName === test.expected ? 'PASS' : 'FAIL'}`);
    console.log('---');
});
```

**Интеграционное тестирование:**

1. Запустить анализ графа вызовов на реальных примерах:
   - Node.js модули с `module.exports`
   - CommonJS паттерны
   - Смешанные случаи с `exports` и локальными переменными

2. Проверить вывод:
   - JSON формат корректный
   - Имена функций соответствуют ожиданиям
   - Связи вызовов построены правильно

**Регрессионное тестирование:**

1. Запустить весь набор существующих тестов: `npm test`
2. Проверить тесты в директориях:
   - `tests/input/import-export/module.exports/`
   - `tests/input/basics/`
   - `tests/input/es6/`

3. Убедиться, что изменения не сломали другую функциональность

**Граничные случаи для тестирования:**

1. `exports.fn = function() {}` - базовый случай ✓
2. `module.exports.fn = function() {}` - вложенный MemberExpression ✓
3. `exports['computed'] = function() {}` - computed property (должен игнорироваться)
4. `exports.fn = () => {}` - arrow функция ✓
5. `obj.nested.deep.fn = function() {}` - глубокая вложенность (вернуть `"fn"`)
6. Смешанные случаи в одном файле

## 🎯 Критерии успеха

Функция считается готовой и работающей корректно, если:

1. **Основная функциональность работает:**
   - Конструкция `exports.fn = function() {}` генерирует имя функции `"fn"` вместо `"global:anon[1]"`
   - Конструкция `module.exports.fn = function() {}` генерирует имя функции `"fn"`
   - Arrow функции `exports.handler = () => {}` также получают корректное имя `"handler"`

2. **Все тесты проходят:**
   - Новые тесты для `export.fn` паттерна проходят успешно
   - Существующие тесты продолжают работать без регрессий
   - `npm test` завершается без ошибок

3. **Консистентность поведения:**
   - Поведение одинаково для `FunctionExpression` и `ArrowFunctionExpression`
   - Computed properties (`exports[expr]`) корректно игнорируются
   - Глубокие вложенности (`a.b.c.fn`) возвращают имя последнего свойства

4. **Код качественный:**
   - Изменения минимальны и локализованы
   - Код следует существующим паттернам проекта
   - Добавлены комментарии для объяснения логики

5. **Документация обновлена:**
   - Эталонные файлы (truth files) созданы или обновлены
   - Если изменилось поведение существующих тестов, это задокументировано
